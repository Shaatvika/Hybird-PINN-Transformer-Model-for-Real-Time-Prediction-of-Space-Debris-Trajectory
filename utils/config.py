class Config:
    def __init__(self, batch_size, epochs, learning_rate, model_path):
        self.batch_size = batch_size
        self.epochs = epochs
        self.learning_rate = learning_rate
        self.model_path = model_path
